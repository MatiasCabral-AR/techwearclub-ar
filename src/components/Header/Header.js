const Header = ({title}) => {
    return (
        <header className="d-flex justify-content-center align-items-center">
            <h1 className="f-futurismL text-center text-dark">{title}</h1>    
        </header>
    )
}

export default Header